on:
  workflow_call:
    inputs:
      configuration:
        required: true 
        type: string
        default: 'foo'

jobs:
  vulnerable:
    name: a vulnerable workflow
    runs-on: ubuntu-latest
    env:
        configuration: ${{ inputs.configuration }}
    steps:
      - name: echo configuration "${configuration}"
        run: echo Hello "${configuration}"

